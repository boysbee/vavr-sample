package sample.option;

import lombok.val;
import org.junit.Test;

public class RoomTest {
  @Test
  public void test() {
    val room = new Room();
//    val students = room.getStudents().flatMap(Student::getLocation).flatMap()
  }
}